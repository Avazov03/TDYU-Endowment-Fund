import fs from 'node:fs'
const h = fs.readFileSync('public/cyan/index.html', 'utf8')
const red = [...h.matchAll(/menu-item-text" style="color:#dc2626[^"]*">([^<]+)/g)].map((m) => m[1])
const ok = [...h.matchAll(/menu-item-text">([^<]+)/g)].map((m) => m[1])
console.log('RED (unique):')
;[...new Set(red)].forEach((l) => console.log(' ', l))
console.log('\nNORMAL (unique, first 40):')
;[...new Set(ok)].slice(0, 40).forEach((l) => console.log(' ', l))
console.log('\ncounts red', red.length, 'normal', ok.length)
console.log('has css link', h.includes('tdyu-menu-mark.css'))
