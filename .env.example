DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-to-a-long-random-string"
ADMIN_EMAIL="admin@tdyu-endowment.uz"
ADMIN_PASSWORD="Admin123!"
ADMIN_NAME="TDYU Admin"
# Super admin (productionda alohida kuchli parol — faqat server .env)
SUPER_ADMIN_EMAIL="super@example.com"
SUPER_ADMIN_PASSWORD="change-me-min-8-chars"
SUPER_ADMIN_NAME="Super admin"
# Local namuna (git). Production qiymatlarni faqat serverdagi .env ga yozing — bu faylga emas.
PORT=8787
HOST=0.0.0.0
# CORS_ORIGIN bo‘sh = barcha origin (faqat local). Qiymat bo‘lsa: vergul bilan, https://, oxirida / yo‘q.
# Production (PM2, gitga tushmaydi): HOST=127.0.0.1, PORT=18787,
# CORS_ORIGIN=https://<tdyu-subdomen>,https://<ikkinchi-nom>  (masalan yuretta/yuritta juftligi).
CORS_ORIGIN=
